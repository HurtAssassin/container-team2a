apiVersion: v1
kind: Service
metadata:
  name: app-service-external
  namespace: container-team2a
spec:
  selector:
    app: app-web
  ports:
    - name: http
      port: 80
      targetPort: 5000
  type: LoadBalancer
